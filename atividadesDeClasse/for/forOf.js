/**
 * Dado um array de números [3, 7, 2, 9, 5], crie um loop "for" que itere 
 * pelo array e exiba cada elemento no console.
 */

const numeros = [3, 7, 2, 9, 5]

for (const value of numeros) {
    console.log(value);
}